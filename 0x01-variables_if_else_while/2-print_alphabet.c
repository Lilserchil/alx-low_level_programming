#include <stdio.h>
#include <ctype,h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Succes)
 */
int main(void)
{
int lowerCase = 'a';
while (lowerCase <= 'z')
{
putchar(lowerCase);
lowercase += 1;
}
putchar("\n");
return (0);
}
